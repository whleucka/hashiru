name: lint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: shellcheck
        run: |
          shellcheck --version
          shellcheck -x --source-path=. \
            install.sh \
            lib/common.sh \
            scripts/*.sh \
            iso/build.sh \
            iso/test-qemu.sh \
            iso/firstboot/*.sh \
            iso/overlay/airootfs/root/stage0.sh \
            config/profile.d/hashiru-report.sh
