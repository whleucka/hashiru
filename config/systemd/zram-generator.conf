# Hashiru zram swap configuration
# zram-size = ram → compressed swap sized to total RAM
# (zstd typically achieves ~3:1, so this rarely exhausts memory)
[zram0]
zram-size = ram
compression-algorithm = zstd
