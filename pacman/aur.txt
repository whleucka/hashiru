# AUR packages
# Requires yay (bootstrapped separately in 20-aur.sh)

# AUR helper
yay-bin

# LSPs and dev tools. Nothing here compiles: both LSPs are npm shims.
# sqls (SQL LSP) was dropped in v1.7.2 — a Go build on every fresh install, for
# a language server nothing in this repo configures. Install it by hand if an
# editor config wants it.
claude-code
nodejs-intelephense

# Benchmarking
wrk

# Media
spotify

# Default terminal launcher (GLib/Thunar "open in terminal" apps -> kitty,
# configured via thunar/.config/xdg-terminals.list in dotfiles)
xdg-terminal-exec
