# Development tools

# Version control
github-cli

# Editor (stable neovim from extra)
neovim
# tree-sitter-cli: nvim-treesitter (main) shells out to `tree-sitter build`
# to compile parsers. The `tree-sitter` package is only the library, so
# without this every :TSInstall fails with ENOENT: 'tree-sitter'.
tree-sitter-cli

# Build essentials
base-devel
cmake
ninja
meson

# C/C++
clang
llvm
lldb
gdb

# Rust
rustup

# C libraries (graphics)
glfw
glm

# Python
python
python-pip
python-pipx
uv

# Node (via mise, but npm fallback)
nodejs
npm
pnpm

# PHP
php
php-sqlite
composer

# Containers
docker
docker-compose
docker-buildx

# LLMs (claude-code is AUR — see aur.txt)
opencode

# LSPs (from official repos; intelephense is AUR — see aur.txt)
lua-language-server
bash-language-server
typescript-language-server
yaml-language-server
pyright
taplo-cli
vscode-css-languageserver
vscode-html-languageserver

# Linters
ruff

# Formatters
stylua
shfmt
prettier
