# Wayland desktop stack

# Hyprland
hyprland
hyprlock
hypridle
hyprpicker
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk

# Vulkan drivers (install all, only the matching hardware's ICD loads — same
# reasoning as intel-ucode + amd-ucode in base.txt). mesa arrives as a Hyprland
# dependency but ships GL only, so without an ICD here there is no Vulkan at
# all: wgpu apps fall back to GL and abort on the first surface configure, which
# takes out the `confetti` fanfare in update-system. No lib32 — those matter
# only to 32-bit Steam titles, which degrade to GL rather than failing.
vulkan-radeon
vulkan-intel
vulkan-nouveau

# GTK theming (Adwaita-dark variant for GTK3 apps)
gnome-themes-extra

# Audio (PipeWire)
pipewire
pipewire-alsa
pipewire-pulse
pipewire-jack
wireplumber
pavucontrol

# Screen tools
grim
slurp
wl-clipboard
# Clipboard persistence — fixes cliphist races with kitty selection
wl-clip-persist
cliphist

# Bar and launcher
waybar
fuzzel
# socat: waybar ws-listener subscribes to Hyprland's event socket via socat
socat

# Wallpaper
awww

# Notifications
mako

# Qt Wayland
qt5-wayland
qt6-wayland

# File manager
yazi
thunar
thunar-archive-plugin
thunar-volman
gvfs
tumbler
ffmpegthumbnailer

# Polkit
polkit-gnome

# Hardware control (brightness, media keys)
swayosd
brightnessctl
playerctl

# Other
calcurse
# calcurse-caldav (Google Calendar sync): httplib2 is a hard import,
# oauth2client is needed for AuthMethod = oauth2
python-httplib2
python-oauth2client
